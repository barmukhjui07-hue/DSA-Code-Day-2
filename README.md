~~ 🚀 Data Structures & Algorithms Showcase:
Daily tracking of DSA problems, C++ implementation, and core concepts.

## 📌 Day 2: C++ Operators & Variable Mutation
~~ 🧠 Concepts Used:
1. *Arithmetic Operators:* Performing basic operations (`+`, `-`, `*`, `/`, `%`) on integer variables.
2. *Pre-Increment (`++a`):* Increasing variable value before evaluating the assignment statement.
3. *Post-Decrement (`a--`):* Assigning the current variable value before performing the decrement step.

~~ 🛠️ Skills Learnt:
* Understanding unary vs binary operators in C++.
* Tracking order of execution in expression evaluation.
* Predicting state changes during variable mutation.

~~ 💡 Key Takeaways:
1. **Pre vs Post Operators:** `int b = ++a` increments `a` first then assigns to `b` (both become 11). `int b = a--` assigns current `a` to `b` first (b becomes 10), then decrements `a` (a becomes 9).
2. **Integer Division & Modulo:** Integer division (`a / b`) truncates fractional components, while modulo (`a % b`) yields the remainder.
